"use strict";(()=>{function x(){let n=document.querySelectorAll("script[data-api-key]"),e=n[n.length-1],s=e?.dataset.apiKey??"";return s||console.error("[SentryChat] Missing required data-api-key attribute."),{apiKey:s,name:e?.dataset.name??"AI Assistant",accent:e?.dataset.accent??"#FF5B1F",position:e?.dataset.position??"right",greeting:e?.dataset.greeting??"Hi there! I'm an AI assistant. How can I help you today?"}}function v(n){let{accent:e,position:s}=n,t=s==="left";return`
    /* ── Reset ─────────────────────────────────────────────────────────── */
    *, *::before, *::after {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    :host {
      /* Scoped custom properties */
      --sc-ink: #0a0a0c;
      --sc-ink-2: #111114;
      --sc-ink-3: #1a1a1f;
      --sc-bone: #F5F1E8;
      --sc-bone-dim: #a8a49c;
      --sc-accent: ${e};
      --sc-radius-panel: 20px;
      --sc-radius-bubble: 9999px;
      --sc-radius-msg: 16px;
      --sc-shadow: 0 8px 32px rgba(0,0,0,.45);
      --sc-font: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI",
                  Roboto, Oxygen, Ubuntu, Cantarell, "Helvetica Neue", sans-serif;

      font-family: var(--sc-font);
      font-size: 15px;
      line-height: 1.5;
      color: var(--sc-bone);
      -webkit-font-smoothing: antialiased;
      -moz-osx-font-smoothing: grayscale;
    }

    /* ── Floating trigger bubble ────────────────────────────────────── */
    .trigger {
      position: fixed;
      bottom: 20px;
      ${t?"left: 20px":"right: 20px"};
      z-index: 2147483646; /* just below max */
      width: 56px;
      height: 56px;
      border-radius: var(--sc-radius-bubble);
      background: var(--sc-accent);
      border: none;
      cursor: pointer;
      display: flex;
      align-items: center;
      justify-content: center;
      box-shadow: 0 4px 24px rgba(0,0,0,.35);
      transition: transform .25s cubic-bezier(.22,1,.36,1),
                  box-shadow .25s cubic-bezier(.22,1,.36,1);
      outline: none;
    }
    .trigger:hover {
      transform: scale(1.08);
      box-shadow: 0 6px 32px rgba(0,0,0,.5);
    }
    .trigger:focus-visible {
      outline: 2px solid var(--sc-accent);
      outline-offset: 3px;
    }
    .trigger svg {
      width: 26px;
      height: 26px;
      fill: #fff;
      transition: transform .3s cubic-bezier(.22,1,.36,1);
    }
    .trigger[aria-expanded="true"] svg {
      transform: rotate(90deg) scale(.85);
    }

    /* ── Chat panel ────────────────────────────────────────────────── */
    .panel {
      position: fixed;
      bottom: 88px;
      ${t?"left: 20px":"right: 20px"};
      z-index: 2147483646;
      width: 380px;
      max-width: calc(100vw - 32px);
      height: 540px;
      max-height: calc(100vh - 120px);
      border-radius: var(--sc-radius-panel);
      background: rgba(10, 10, 12, .82);
      backdrop-filter: blur(24px);
      -webkit-backdrop-filter: blur(24px);
      border: 1px solid rgba(245, 241, 232, .08);
      box-shadow: var(--sc-shadow),
                  inset 0 1px 0 rgba(245, 241, 232, .06);
      display: flex;
      flex-direction: column;
      overflow: hidden;

      /* Animate open / close */
      opacity: 0;
      transform: translateY(12px) scale(.96);
      pointer-events: none;
      transition: opacity .25s cubic-bezier(.22,1,.36,1),
                  transform .25s cubic-bezier(.22,1,.36,1);
    }
    .panel.open {
      opacity: 1;
      transform: translateY(0) scale(1);
      pointer-events: auto;
    }

    /* Solid fallback for browsers without backdrop-filter */
    @supports not (backdrop-filter: blur(24px)) {
      .panel { background: var(--sc-ink); }
    }

    /* ── Header ────────────────────────────────────────────────────── */
    .header {
      display: flex;
      align-items: center;
      gap: 10px;
      padding: 16px 18px;
      border-bottom: 1px solid rgba(245, 241, 232, .06);
      flex-shrink: 0;
    }
    .header-dot {
      width: 10px;
      height: 10px;
      border-radius: 50%;
      background: var(--sc-accent);
      flex-shrink: 0;
      /* Breathing pulse animation */
      animation: pulse 2.4s ease-in-out infinite;
    }
    @keyframes pulse {
      0%, 100% { opacity: 1; }
      50% { opacity: .45; }
    }
    .header-name {
      font-weight: 600;
      font-size: 14px;
      letter-spacing: .01em;
      flex: 1;
    }
    .header-close {
      width: 28px;
      height: 28px;
      border-radius: 8px;
      border: none;
      background: transparent;
      color: var(--sc-bone-dim);
      cursor: pointer;
      display: flex;
      align-items: center;
      justify-content: center;
      transition: background .15s, color .15s;
    }
    .header-close:hover {
      background: rgba(245, 241, 232, .08);
      color: var(--sc-bone);
    }
    .header-close:focus-visible {
      outline: 2px solid var(--sc-accent);
      outline-offset: 2px;
    }
    .header-close svg {
      width: 16px;
      height: 16px;
    }

    /* ── Messages area ─────────────────────────────────────────────── */
    .messages {
      flex: 1;
      overflow-y: auto;
      padding: 16px 14px;
      display: flex;
      flex-direction: column;
      gap: 10px;
      scroll-behavior: smooth;
      /* Custom scrollbar */
      scrollbar-width: thin;
      scrollbar-color: rgba(245,241,232,.12) transparent;
    }
    .messages::-webkit-scrollbar {
      width: 5px;
    }
    .messages::-webkit-scrollbar-track {
      background: transparent;
    }
    .messages::-webkit-scrollbar-thumb {
      background: rgba(245,241,232,.12);
      border-radius: 3px;
    }

    /* ── Message bubbles ───────────────────────────────────────────── */
    .msg {
      max-width: 85%;
      padding: 10px 14px;
      border-radius: var(--sc-radius-msg);
      font-size: 14px;
      line-height: 1.55;
      word-break: break-word;
      animation: msgIn .3s cubic-bezier(.22,1,.36,1) both;
    }
    @keyframes msgIn {
      from { opacity: 0; transform: translateY(6px); }
      to   { opacity: 1; transform: translateY(0); }
    }
    .msg.user {
      align-self: flex-end;
      background: var(--sc-accent);
      color: #fff;
      border-bottom-right-radius: 4px;
    }
    .msg.assistant {
      align-self: flex-start;
      background: var(--sc-ink-3);
      color: var(--sc-bone);
      border-bottom-left-radius: 4px;
    }

    /* Markdown-lite: basic inline formatting in assistant messages */
    .msg.assistant code {
      font-family: "SFMono-Regular", Consolas, "Liberation Mono", Menlo, monospace;
      font-size: 13px;
      background: rgba(245,241,232,.07);
      padding: 1px 5px;
      border-radius: 4px;
    }
    .msg.assistant a {
      color: var(--sc-accent);
      text-decoration: underline;
      text-underline-offset: 2px;
    }

    /* Streaming cursor blink */
    .msg.assistant.streaming::after {
      content: "▍";
      animation: blink .6s steps(2) infinite;
      margin-left: 1px;
      color: var(--sc-accent);
    }
    @keyframes blink {
      50% { opacity: 0; }
    }

    /* Typing indicator (three dots) */
    .typing {
      display: flex;
      gap: 5px;
      align-self: flex-start;
      padding: 12px 18px;
      background: var(--sc-ink-3);
      border-radius: var(--sc-radius-msg);
      border-bottom-left-radius: 4px;
      animation: msgIn .3s cubic-bezier(.22,1,.36,1) both;
    }
    .typing span {
      width: 7px;
      height: 7px;
      border-radius: 50%;
      background: var(--sc-bone-dim);
      animation: typingDot 1.2s ease-in-out infinite;
    }
    .typing span:nth-child(2) { animation-delay: .15s; }
    .typing span:nth-child(3) { animation-delay: .3s; }
    @keyframes typingDot {
      0%, 60%, 100% { opacity: .3; transform: translateY(0); }
      30% { opacity: 1; transform: translateY(-4px); }
    }

    /* ── Input area ────────────────────────────────────────────────── */
    .input-area {
      display: flex;
      align-items: flex-end;
      gap: 8px;
      padding: 12px 14px;
      border-top: 1px solid rgba(245, 241, 232, .06);
      flex-shrink: 0;
    }
    .input-area textarea {
      flex: 1;
      resize: none;
      border: none;
      outline: none;
      background: var(--sc-ink-3);
      color: var(--sc-bone);
      font-family: var(--sc-font);
      font-size: 14px;
      line-height: 1.45;
      padding: 10px 14px;
      border-radius: 12px;
      min-height: 40px;
      max-height: 120px;
      overflow-y: auto;
      scrollbar-width: none;
    }
    .input-area textarea::-webkit-scrollbar { display: none; }
    .input-area textarea::placeholder {
      color: var(--sc-bone-dim);
    }
    .input-area textarea:focus-visible {
      box-shadow: 0 0 0 2px var(--sc-accent);
    }

    .send-btn {
      width: 40px;
      height: 40px;
      border-radius: 12px;
      border: none;
      background: var(--sc-accent);
      color: #fff;
      cursor: pointer;
      display: flex;
      align-items: center;
      justify-content: center;
      flex-shrink: 0;
      transition: opacity .15s, transform .15s;
    }
    .send-btn:disabled {
      opacity: .4;
      cursor: not-allowed;
    }
    .send-btn:not(:disabled):hover {
      transform: scale(1.06);
    }
    .send-btn:focus-visible {
      outline: 2px solid var(--sc-accent);
      outline-offset: 2px;
    }
    .send-btn svg {
      width: 18px;
      height: 18px;
    }

    /* ── Powered-by footer ─────────────────────────────────────────── */
    .footer {
      text-align: center;
      padding: 6px 14px 10px;
      font-size: 11px;
      color: var(--sc-bone-dim);
      opacity: .6;
      flex-shrink: 0;
    }
    .footer a {
      color: var(--sc-bone-dim);
      text-decoration: none;
    }
    .footer a:hover {
      text-decoration: underline;
    }

    /* ── Mobile adjustments ────────────────────────────────────────── */
    @media (max-width: 480px) {
      .panel {
        width: calc(100vw - 16px);
        height: calc(100vh - 100px);
        bottom: 78px;
        ${t?"left: 8px":"right: 8px"};
        border-radius: 16px;
      }
      .trigger {
        bottom: 14px;
        ${t?"left: 14px":"right: 14px"};
        width: 50px;
        height: 50px;
      }
    }

    /* ── Reduced motion ────────────────────────────────────────────── */
    @media (prefers-reduced-motion: reduce) {
      .panel,
      .trigger,
      .trigger svg,
      .msg,
      .typing span {
        animation: none !important;
        transition-duration: 0.01ms !important;
      }
    }
  `}var y='<svg viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path d="M20 2H4c-1.1 0-2 .9-2 2v18l4-4h14c1.1 0 2-.9 2-2V4c0-1.1-.9-2-2-2zm0 14H5.17L4 17.17V4h16v12z" fill="currentColor"/><path d="M7 9h10v2H7zm0-3h10v2H7z" fill="currentColor"/></svg>',w='<svg viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path d="M18.3 5.71a1 1 0 00-1.41 0L12 10.59 7.11 5.7A1 1 0 005.7 7.11L10.59 12 5.7 16.89a1 1 0 101.41 1.41L12 13.41l4.89 4.89a1 1 0 001.41-1.41L13.41 12l4.89-4.89a1 1 0 000-1.4z" fill="currentColor"/></svg>',k='<svg viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path d="M3.4 20.4l17.45-7.48a1 1 0 000-1.84L3.4 3.6a.993.993 0 00-1.39.91L2 9.12c0 .5.37.93.87.99L17 12 2.87 13.88c-.5.07-.87.5-.87 1l.01 4.61c0 .71.73 1.2 1.39.91z" fill="currentColor"/></svg>';async function*E(n,e){let s=e.map(r=>({role:r.role==="assistant"?"model":"user",parts:[{text:r.content}]})),t=`https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash:streamGenerateContent?key=${encodeURIComponent(n)}&alt=sse`,a=await fetch(t,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({contents:s,generationConfig:{temperature:.7,topP:.95,maxOutputTokens:2048}})});if(!a.ok){let r=await a.text();throw new Error(`Gemini API error ${a.status}: ${r}`)}let i=a.body?.getReader();if(!i)throw new Error("No readable stream from Gemini response.");let o=new TextDecoder,l="";for(;;){let{done:r,value:g}=await i.read();if(r)break;l+=o.decode(g,{stream:!0});let d=l.split(`
`);l=d.pop()??"";for(let f of d){let h=f.trim();if(!h.startsWith("data:"))continue;let m=h.slice(5).trim();if(m==="[DONE]")return;try{let u=JSON.parse(m)?.candidates?.[0]?.content?.parts?.[0]?.text??"";u&&(yield u)}catch{}}}if(l.trim().startsWith("data:")){let r=l.trim().slice(5).trim();if(r&&r!=="[DONE]")try{let d=JSON.parse(r)?.candidates?.[0]?.content?.parts?.[0]?.text??"";d&&(yield d)}catch{}}}function c(n){return n.replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/`([^`]+)`/g,"<code>$1</code>").replace(/\*\*(.+?)\*\*/g,"<strong>$1</strong>").replace(/__(.+?)__/g,"<strong>$1</strong>").replace(/\*(.+?)\*/g,"<em>$1</em>").replace(/_(.+?)_/g,"<em>$1</em>").replace(/\[([^\]]+)\]\((https?:\/\/[^)]+)\)/g,'<a href="$2" target="_blank" rel="noopener noreferrer">$1</a>').replace(/\n/g,"<br>")}var p=class{constructor(e){this.messages=[];this.isOpen=!1;this.isStreaming=!1;this.abortController=null;this.config=e,this.host=document.createElement("div"),this.host.id="sentry-chat-widget",this.shadow=this.host.attachShadow({mode:"open"}),this.buildDOM(),this.bindEvents(),document.body.appendChild(this.host)}buildDOM(){let{name:e}=this.config,s=document.createElement("style");s.textContent=v(this.config),this.shadow.appendChild(s),this.trigger=document.createElement("button"),this.trigger.className="trigger",this.trigger.setAttribute("aria-label",`Open ${e} chat`),this.trigger.setAttribute("aria-expanded","false"),this.trigger.innerHTML=y,this.shadow.appendChild(this.trigger),this.panel=document.createElement("div"),this.panel.className="panel",this.panel.setAttribute("role","dialog"),this.panel.setAttribute("aria-label",`${e} chat`);let t=document.createElement("div");t.className="header",t.innerHTML=`
      <span class="header-dot" aria-hidden="true"></span>
      <span class="header-name">${this.escapeHtml(e)}</span>
    `;let a=document.createElement("button");a.className="header-close",a.setAttribute("aria-label","Close chat"),a.innerHTML=w,a.addEventListener("click",()=>this.toggle(!1)),t.appendChild(a),this.panel.appendChild(t),this.messagesEl=document.createElement("div"),this.messagesEl.className="messages",this.messagesEl.setAttribute("role","log"),this.messagesEl.setAttribute("aria-live","polite"),this.panel.appendChild(this.messagesEl);let i=document.createElement("div");i.className="input-area",this.textarea=document.createElement("textarea"),this.textarea.placeholder="Type a message…",this.textarea.rows=1,this.textarea.setAttribute("aria-label","Chat message input"),this.sendBtn=document.createElement("button"),this.sendBtn.className="send-btn",this.sendBtn.setAttribute("aria-label","Send message"),this.sendBtn.innerHTML=k,this.sendBtn.disabled=!0,i.appendChild(this.textarea),i.appendChild(this.sendBtn),this.panel.appendChild(i);let o=document.createElement("div");o.className="footer",o.innerHTML='Powered by <a href="https://ai.google.dev" target="_blank" rel="noopener noreferrer">Gemini</a>',this.panel.appendChild(o),this.shadow.appendChild(this.panel)}bindEvents(){this.trigger.addEventListener("click",()=>this.toggle()),this.sendBtn.addEventListener("click",()=>this.handleSend()),this.textarea.addEventListener("input",()=>{this.autoResize(),this.sendBtn.disabled=this.textarea.value.trim().length===0}),this.textarea.addEventListener("keydown",e=>{e.key==="Enter"&&!e.shiftKey&&(e.preventDefault(),this.handleSend())}),this.shadow.addEventListener("keydown",e=>{e.key==="Escape"&&this.isOpen&&(this.toggle(!1),this.trigger.focus())}),document.addEventListener("keydown",e=>{e.key==="Escape"&&this.isOpen&&(this.toggle(!1),this.trigger.focus())})}toggle(e){this.isOpen=e??!this.isOpen,this.panel.classList.toggle("open",this.isOpen),this.trigger.setAttribute("aria-expanded",String(this.isOpen)),this.isOpen&&(this.messages.length===0&&this.config.greeting&&this.appendMessage("assistant",this.config.greeting),requestAnimationFrame(()=>this.textarea.focus()))}async handleSend(){let e=this.textarea.value.trim();if(!e||this.isStreaming)return;if(!this.config.apiKey){this.appendMessage("assistant","⚠ No API key configured. Add `data-api-key` to the widget script tag.");return}this.appendMessage("user",e),this.textarea.value="",this.autoResize(),this.sendBtn.disabled=!0;let s=this.showTypingIndicator();this.isStreaming=!0,this.abortController=new AbortController;let t=this.createBubble("assistant","");t.classList.add("streaming");try{let a=!1,i="";for await(let o of E(this.config.apiKey,this.messages))a||(s.remove(),a=!0,this.messagesEl.appendChild(t)),i+=o,t.innerHTML=c(i),this.scrollToBottom();a||(s.remove(),this.messagesEl.appendChild(t)),t.classList.remove("streaming"),this.messages.push({role:"assistant",content:i})}catch(a){s.remove(),t.classList.remove("streaming");let i=a instanceof Error?a.message:"An unexpected error occurred.";t.innerHTML=c(`⚠ Sorry, something went wrong: ${i}`),t.parentNode||this.messagesEl.appendChild(t)}finally{this.isStreaming=!1,this.abortController=null,this.scrollToBottom()}}appendMessage(e,s){this.messages.push({role:e,content:s});let t=this.createBubble(e,s);this.messagesEl.appendChild(t),this.scrollToBottom()}createBubble(e,s){let t=document.createElement("div");return t.className=`msg ${e}`,t.innerHTML=e==="user"?this.escapeHtml(s):c(s),t}showTypingIndicator(){let e=document.createElement("div");return e.className="typing",e.setAttribute("aria-label","Thinking…"),e.innerHTML="<span></span><span></span><span></span>",this.messagesEl.appendChild(e),this.scrollToBottom(),e}scrollToBottom(){requestAnimationFrame(()=>{this.messagesEl.scrollTop=this.messagesEl.scrollHeight})}autoResize(){this.textarea.style.height="auto",this.textarea.style.height=`${Math.min(this.textarea.scrollHeight,120)}px`}escapeHtml(e){let s=document.createElement("span");return s.textContent=e,s.innerHTML}};function b(){let n=x();new p(n)}document.readyState==="loading"?document.addEventListener("DOMContentLoaded",b):b();})();
